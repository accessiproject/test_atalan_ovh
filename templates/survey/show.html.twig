{% extends 'base.html.twig' %}

{% block title %}
    Consultation
{% endblock %}

{% block body %}
    {% include 'partials/form_answer.html.twig' %}
    <div class='form-group'>
        <hr>
        <a role="button" class="btn btn-info" href="{{ path('survey_edit', {'id': survey.id}) }}">Modifier</a>
        <a role="button" class="btn btn-danger" href="">Supprimer</a>
        <a role="button" class="btn btn-info" href="{{ path('survey_list') }}">Retour aux autres sondages</a>
    </div>
{% endblock %}