<nav role="navigation" aria-label="Menu secondaire">
    <ul>
        {% set currentPath = path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) %}
        {% if "sondage/" in currentPath %}
            <li>
                <a href="{{ path('survey_list') }}">Voir la liste des sondages</a>
            </li>
            <li>
                <a href="{{ path('survey_edit', {'id': id}) }}">Modifier</a>
            </li>
            <li>
                <a href="{{ path('survey_delete', {'id': id}) }}">Supprimer</a>
            </li>
            <li>
                <a href="{{ path('answer_list', {'id': id}) }}">Gérer les réponses</a>
            </li>
            <li>
                <a href="{{ path('survey_result', {'id': id}) }}">Voir les résultats</a>
            </li>
            <li>
                <a target="_blank" href="{{ path('answer_survey', {'id': id}) }}">Répondre (en mode public)</a>
            </li>
        {% else %}
            <p>kevin</p>
        {% endif %}
    </ul>
</nav>