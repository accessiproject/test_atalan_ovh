{% if app.user %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light" role="navigation" aria-label="Menu principal">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('survey_list') }}">Sondage</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('assistive_list') }}">Technologie d'assistance</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('user_list') }}">Administrateur</a>
            </li>
            <ul class="nav navbar-nav navbar-right">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            </ul>
        </ul>
    </nav>
{% endif %}
<script>
    let elements = document.querySelectorAll("nav > ul > li > a");
    let url = window.location.href;
    console.log(url);
    for (let i=0;i<elements.length;i++) {
        console.log(elements[i]);
        if (elements[i].href==url) {
            elements[i].setAttribute("aria-current", "page");
            elements[i].className="active";
        } else {
            elements[i].className="nav-link";
        }
            
    }
</script>